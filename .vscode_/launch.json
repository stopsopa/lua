{
  "version": "0.2.0",
  "configurations": [
    {
        "type": "lrdb",
        "request": "launch",
        "name": "Lua Launch",
        "program": "${fileDirname}/${fileBasenameNoExtension}.lua",
        "cwd": "${workspaceFolder}",
        // "args": ["ab", "c d"],
        "args": ["--stdinfile", "input.txt"],        
        "stopOnEntry": true
    }
  ]
}

